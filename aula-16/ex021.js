function soma(n1=0, n2=0) {
    return n1+n2
}

console.log(soma(9,8))

console.log(soma(4))

/* declarou os parametros como igual a 0 porque assim o valor base será 0, para caso a pessoa insira apenas um valor. se não tivesse isso e a pessoa
colocasse um número só teria como return NaN */